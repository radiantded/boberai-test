### BoberAI Test Task

Тестовый backend-сервис на FastAPI, который принимает задачи через HTTP API и обрабатывает их асинхронно с использованием очереди сообщений и PostgreSQL

## Стек технологий

- FastAPI
- FastStream + RabbitMQ
- PostgreSQL
- SQLAlchemy
- Docker / docker compose

## Запуск

```docker compose up --build -d```

Документация API будет доступна по адресу:

```http://localhost:8000/docs```

## Почему выбран RabbitMQ

- простая модель: producer → consumer
- лёгкий запуск
- нет опыта с Kafka 🦫

## Масштабирование решения

- несколько воркеров, читающих из одной очереди
- разные очереди для разных типов задач
- несколько экземпляров API за балансировщиком
- добавить connection pooling для бд
- добавить реплики на чтение

## Потенциальные точки отказа

- единственный брокер сообщений
- отказ БД
- задача может застрять в состоянии failed из-за отсутствия retry-логики
- повторная обработка сообщения может привести к дублированию (отсутствие идемпотентности)

## Что можно добавить

- Retry и Dead Letter Queue (DLQ) для неуспешных задач
- идемпотентность обработки задач
- миграции Alembic
- мониторинг (Grafana, Prometheus)
- лимит запросов для API
- тесты